<?php

use Acheteteper\Utils\ViewUtils as v;

$title = 'Validation Demo';
?>
<style>
    form {
        margin: 20px 0;
    }

    .form-group {
        margin: 15px 0;
    }

    label {
        display: block;
        margin-bottom: 5px;
        font-weight: bold;
    }

    input {
        width: 100%;
        padding: 8px;
        box-sizing: border-box;
    }

    .error {
        color: red;
        font-size: 0.9em;
        margin-top: 5px;
    }

    .success {
        color: green;
        padding: 15px;
        background: #d4edda;
        border: 1px solid #c3e6cb;
        margin: 20px 0;
    }

    button {
        background: #007bff;
        color: white;
        padding: 10px 20px;
        border: none;
        cursor: pointer;
    }
</style>

<?php if (isset($success) && $success): ?>
    <div class="success">
        <strong>Success!</strong> <?= v::e($message) ?>
    </div>
<?php endif; ?>

<h2>Registration Form</h2>
<form method="POST" action="/validation/submit">
    <div class="form-group">
        <label>Name *</label>
        <input type="text" name="name" value="<?= v::e($values['name'] ?? '') ?>" required>
        <?php if (isset($errors['name'])): ?>
            <div class="error"><?= v::e($errors['name']) ?></div>
        <?php endif; ?>
    </div>

    <div class="form-group">
        <label>Email *</label>
        <input type="email" name="email" value="<?= v::e($values['email'] ?? '') ?>" required>
        <?php if (isset($errors['email'])): ?>
            <div class="error"><?= v::e($errors['email']) ?></div>
        <?php endif; ?>
    </div>

    <div class="form-group">
        <label>Phone *</label>
        <input type="text" name="phone" value="<?= v::e($values['phone'] ?? '') ?>" placeholder="+1-555-123-4567" required>
        <?php if (isset($errors['phone'])): ?>
            <div class="error"><?= v::e($errors['phone']) ?></div>
        <?php endif; ?>
    </div>

    <div class="form-group">
        <label>Password * (min 8 characters)</label>
        <input type="password" name="password" required>
        <?php if (isset($errors['password'])): ?>
            <div class="error"><?= v::e($errors['password']) ?></div>
        <?php endif; ?>
    </div>

    <div class="form-group">
        <label>Confirm Password *</label>
        <input type="password" name="password_confirm" required>
        <?php if (isset($errors['password_confirm'])): ?>
            <div class="error"><?= v::e($errors['password_confirm']) ?></div>
        <?php endif; ?>
    </div>

    <button type="submit">Register</button>
</form>
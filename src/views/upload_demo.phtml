<?php
$title = 'File Upload Demo';
ob_start();
?>

<?php if (isset($success) && $success): ?>
    <div class="success">
        <strong>Success!</strong> File uploaded successfully.
    </div>
    <h2>Uploaded File Information</h2>
    <table>
        <tr>
            <th>Property</th>
            <th>Value</th>
        </tr>
        <tr>
            <td>Original Name</td>
            <td><?= \Acheteteper\e($file['name']) ?></td>
        </tr>
        <tr>
            <td>Size</td>
            <td><?= number_format($file['size'] / 1024, 2) ?> KB</td>
        </tr>
        <tr>
            <td>Type</td>
            <td><?= \Acheteteper\e($file['type']) ?></td>
        </tr>
        <tr>
            <td>Saved As</td>
            <td><?= \Acheteteper\e($file['filename']) ?></td>
        </tr>
    </table>
    <?php if (strpos($file['type'], 'image/') === 0): ?>
        <img src="/uploads/<?= \Acheteteper\e($file['filename']) ?>" alt="Uploaded image">
    <?php endif; ?>
<?php endif; ?>

<?php if (isset($errors) && !empty($errors)): ?>
    <div class="error">
        <strong>Errors:</strong>
        <ul>
            <?php foreach ($errors as $error): ?>
                <li><?= \Acheteteper\e($error) ?></li>
            <?php endforeach; ?>
        </ul>
    </div>
<?php endif; ?>

<h2>Upload File</h2>
<p>Upload an image file (jpg, jpeg, png, gif) - max 2MB</p>
<form method="POST" action="/upload/submit" enctype="multipart/form-data">
    <input type="file" name="file" accept="image/*" required>
    <button type="submit">Upload File</button>
</form>

<?php
$content = ob_get_clean();
$extraStyles = '
    .success { color: green; padding: 15px; background: #d4edda; border: 1px solid #c3e6cb; margin: 20px 0; }
    .error { color: red; padding: 15px; background: #f8d7da; border: 1px solid #f5c6cb; margin: 20px 0; }
    form { margin: 20px 0; padding: 15px; background: #f9f9f9; border: 1px solid #ddd; }
    input, button { padding: 8px; margin: 5px; }
    button { background: #007bff; color: white; border: none; cursor: pointer; }
    table { width: 100%; border-collapse: collapse; margin: 20px 0; }
    th, td { padding: 10px; text-align: left; border: 1px solid #ddd; }
    th { background: #f0f0f0; }
    img { max-width: 300px; margin: 10px 0; }
';
?>
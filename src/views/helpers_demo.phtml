<?php

use Acheteteper\Utils\ViewUtils as v;

$title = 'View Helpers Demo';

?>
<style>
    table {
        width: 100%;
        border-collapse: collapse;
        margin: 20px 0;
    }

    th,
    td {
        padding: 10px;
        text-align: left;
        border: 1px solid #ddd;
    }

    th {
        background: #f0f0f0;
    }

    .example {
        margin: 20px 0;
        padding: 15px;
        background: #f9f9f9;
        border: 1px solid #ddd;
    }

    code {
        background: #f0f0f0;
        padding: 2px 5px;
    }
</style>

<div class="example">
    <h2>e() - HTML Escape</h2>
    <p><strong>Input:</strong> <code><?= v::e($htmlString) ?></code></p>
    <p><strong>Output:</strong> <?= v::e($htmlString) ?></p>
    <p>The script tag is escaped and displayed as text, preventing XSS attacks.</p>
</div>

<div class="example">
    <h2>url() - URL Generation</h2>
    <p><strong>url('/request'):</strong> <code><?= v::url('/request') ?></code></p>
    <p><strong>url('/api/users'):</strong> <code><?= v::url('/api/users') ?></code></p>
</div>

<div class="example">
    <h2>date() - Date Formatting</h2>
    <p><strong>Timestamp:</strong> <?= $timestamp ?></p>
    <p><strong>Default format:</strong> <?= v::date($timestamp) ?></p>
    <p><strong>Custom format (Y-m-d):</strong> <?= v::date($timestamp, 'Y-m-d') ?></p>
    <p><strong>Date string:</strong> <?= $dateString ?></p>
    <p><strong>Formatted:</strong> <?= v::date($dateString, 'F j, Y') ?></p>
</div>

<div class="example">
    <h2>truncate() - String Truncation</h2>
    <p><strong>Original:</strong> <?= v::e($longText) ?></p>
    <p><strong>Truncated (30 chars):</strong> <?= v::truncate($longText, 30) ?></p>
    <p><strong>Truncated (50 chars):</strong> <?= v::truncate($longText, 50) ?></p>
</div>

<div class="example">
    <h2>link() - Link Generation</h2>
    <p><?= v::link('/request', 'Request Demo') ?></p>
    <p><?= v::link('/api/users', 'API Users', ['target' => '_blank']) ?></p>
</div>
<?php

use Acheteteper\Utils\ViewUtils as v;
?>
<?php if ($items === []): ?>
    <p class="text-sm text-zinc-700">No result for <strong><?= v::e($query) ?></strong>.</p>
<?php else: ?>
    <ul class="grid gap-2 sm:grid-cols-2" aria-live="polite">
        <?php foreach ($items as $item): ?>
            <li class="border-l-4 border-blue-500 bg-zinc-50 p-3 font-mono text-sm text-zinc-800 shadow-sm"><?= v::e($item) ?></li>
        <?php endforeach; ?>
    </ul>
<?php endif; ?>
